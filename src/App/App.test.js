import { render, screen } from '@testing-library/react';
import App from './App';

describe("Clicking Movies", () => {
  it("Should allow the user to click a movie and be taken to a different page to view specific details", () => {
    expect(true).to.equal(true)
  })
})